# Stellar Network Configuration
# For testnet:
VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
VITE_RPC_URL=https://soroban-testnet.stellar.org

# For mainnet (uncomment when ready):
# VITE_NETWORK_PASSPHRASE=Public Global Stellar Network ; September 2015
# VITE_RPC_URL=https://soroban.stellar.org

# Contract Configuration
# Replace with your deployed contract ID after running:
#   soroban contract deploy --wasm target/wasm32-unknown-unknown/release/game_of_life.wasm --source deployer --network testnet
VITE_CONTRACT_ID=

# Simulator Address
# Any funded Stellar account address (simulation is free, but needs a valid source account)
# Generate one with: soroban keys generate --global deployer --network testnet
# Then get the address with: soroban keys address deployer
VITE_SIMULATOR_ADDRESS=
